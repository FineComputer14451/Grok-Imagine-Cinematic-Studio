# Onboarding demo board

Copy `onboarding-demo/` to a new project root (or point `cross_ref_check.py` at it).

Contains:

- Character DNA for **Mara Chen** + **Kai Reed** (also under `character-dna-extractor/samples/`)
- Prop **pulse-compass** + location **neon-pier** stubs
- Placeholder plate files + `board/manifest.json`

Replace `PLACEHOLDER_PLATE` files with real Quality / Image 2.0 stills before production lock.
